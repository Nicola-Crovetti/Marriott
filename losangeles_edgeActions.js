/***********************
* Azioni di composizione in Adobe Edge Animate
*
* Modifica con cautela questo file di testo, prestando attenzione a salvare 
* firme e commenti di funzione che iniziano con "Edge" per mantenere 
* possibilità di interagire con le azioni dall'interno di Adobe Edge Animate
*
***********************/
(function($, Edge, compId){
var Composition = Edge.Composition, Symbol = Edge.Symbol; // alias per le classi usate più di frequente in Edge
   //Edge symbol: 'stage'
   (function(symbolName) {
	   Symbol.bindElementAction(compId, symbolName, "${__8x_s}", "click", function(sym, e) {
         // riproduci la linea temporale dalla posizione data (ms o etichetta)
         sym.play(3001);
      });
      //Edge binding end
      Symbol.bindElementAction(compId, symbolName, "${__16x_s}", "click", function(sym, e) {
         // riproduci la linea temporale dalla posizione data (ms o etichetta)
         sym.play(5001);
      });
      //Edge binding end
      Symbol.bindElementAction(compId, symbolName, "${__4x_s}", "click", function(sym, e) {
         // riproduci la linea temporale dalla posizione data (ms o etichetta)
         sym.play(1001);
      });
      //Edge binding end
      Symbol.bindElementAction(compId, symbolName, "${_Stage}", "touchmove", function(sym, e) {
         // inserire qui il codice da eseguire quando l'utente trascina un oggetto (solo per dispositivi touch)
      });
      //Edge binding end
	  
      Symbol.bindElementAction(compId, symbolName, "${__16x_m}", "click", function(sym, e) {
         // riproduci la linea temporale dalla posizione data (ms o etichetta)
         sym.play(12001);
	  });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${__8x_m}", "click", function(sym, e) {
         // riproduci la linea temporale dalla posizione data (ms o etichetta)
         sym.play(10001);
      });
      //Edge binding end
      Symbol.bindElementAction(compId, symbolName, "${__4x_m}", "click", function(sym, e) {
         // riproduci la linea temporale dalla posizione data (ms o etichetta)
         sym.play(8001);
      });
      //Edge binding end
      Symbol.bindElementAction(compId, symbolName, "${__16x_b}", "click", function(sym, e) {
         // riproduci la linea temporale dalla posizione data (ms o etichetta)
         sym.play(19001);
      });
      //Edge binding end
      Symbol.bindElementAction(compId, symbolName, "${__8x_b}", "click", function(sym, e) {
         // riproduci la linea temporale dalla posizione data (ms o etichetta)
         sym.play(17001);
      });
      //Edge binding end
      Symbol.bindElementAction(compId, symbolName, "${__4x_b}", "click", function(sym, e) {
         // riproduci la linea temporale dalla posizione data (ms o etichetta)
         sym.play(15001);
      });
      //Edge binding end
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 1000, function(sym, e) {
         // interrompi la linea temporale nella posizione data (ms o etichetta)
         sym.stop(999);
      });
      //Edge binding end
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 2000, function(sym, e) {
         // riproduci la linea temporale dalla posizione data (ms o etichetta)
         sym.play(0001);
      });
      //Edge binding end
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 4000, function(sym, e) {
         // riproduci la linea temporale dalla posizione data (ms o etichetta)
         sym.play(7001);
      });
      //Edge binding end
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 6000, function(sym, e) {
         // riproduci la linea temporale dalla posizione data (ms o etichetta)
         sym.play(14001);
      });
      //Edge binding end

      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 8000, function(sym, e) {
         // interrompi la linea temporale nella posizione data (ms o etichetta)
         sym.stop(7999);
      });
      //Edge binding end
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 10000, function(sym, e) {
         // inserire codice qui
      });
      //Edge binding end
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 15000, function(sym, e) {
         // interrompi la linea temporale nella posizione data (ms o etichetta)
         sym.stop(14999);
      });
      //Edge binding end
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 9000, function(sym, e) {
         // riproduci la linea temporale dalla posizione data (ms o etichetta)
         sym.play(0001);
      });
      //Edge binding end
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 11000, function(sym, e) {
         // riproduci la linea temporale dalla posizione data (ms o etichetta)
         sym.play(7001);
      });
      //Edge binding end
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 13000, function(sym, e) {
         // riproduci la linea temporale dalla posizione data (ms o etichetta)
         sym.play(14001);
      });
      //Edge binding end
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 16000, function(sym, e) {
         // riproduci la linea temporale dalla posizione data (ms o etichetta)
         sym.play(0001);
      });
      //Edge binding end
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 18000, function(sym, e) {
         // riproduci la linea temporale dalla posizione data (ms o etichetta)
         sym.play(7001);
      });
      //Edge binding end
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 20000, function(sym, e) {
         // riproduci la linea temporale dalla posizione data (ms o etichetta)
         sym.play(14001);
      });
      //Edge binding end
      Symbol.bindElementAction(compId, symbolName, "${_box_b_2}", "click", function(sym, e) {
         // riproduci la linea temporale dalla posizione data (ms o etichetta)
         sym.play(21001);
      });
      //Edge binding end
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 21500, function(sym, e) {
         // interrompi la linea temporale nella posizione data (ms o etichetta)
         sym.stop(21499);
      });
      //Edge binding end
      Symbol.bindElementAction(compId, symbolName, "${_share}", "click", function(sym, e) {
         // riproduci la linea temporale dalla posizione data (ms o etichetta)
         sym.play(21501);
      });
      //Edge binding end
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 22000, function(sym, e) {
         // riproduci la linea temporale dalla posizione data (ms o etichetta)
         sym.play(999);
      });
      //Edge binding end
      Symbol.bindElementAction(compId, symbolName, "${_Booknow-Big2}", "click", function(sym, e) {
         // Passate a un nuovo URL nella finestra attuale
         // (sostituite "_self" con l'attributo di destinazione appropriato per una nuova finestra)
         window.open("losangeles_scroll.html", "_self");
      });
      //Edge binding end
      Symbol.bindElementAction(compId, symbolName, "${_Booknow-Small}", "click", function(sym, e) {
         // Passate a un nuovo URL nella finestra attuale
         // (sostituite "_self" con l'attributo di destinazione appropriato per una nuova finestra)
         window.open("losangeles_scroll.html", "_self");
      });
      //Edge binding end
      Symbol.bindElementAction(compId, symbolName, "${_Booknow-Big}", "click", function(sym, e) {
// Passate a un nuovo URL nella finestra attuale
// (sostituite "_self" con l'attributo di destinazione appropriato per una nuova finestra)
window.open("losangeles_scroll.html", "_self");

      });
      //Edge binding end

   })("stage");
   //Edge symbol end:'stage'

})(jQuery, AdobeEdge, "marriott-los-angeles");